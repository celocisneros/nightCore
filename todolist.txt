Marcelo's Battle Flow Logic 

for my database i am using Native driver = the direct MongoDB Node.js driver

this function will redirect player to the battle.html page

pull the current req.session.player and assign it to a variable named "player"

pull a random monster from the mongodb database named "nightcore" and from the collection "monsters" 
and assign it to a variable named "monster"

place ${player.username} and ${player.hp} in the "playerStats" div

insert an image element with the src attribute being ${monster.profilePicPath} and ${monster.name} and ${monster.hp} in the enemyStats div

need to create a script to update divs whenever a turn ends, and to also update the "turnBox".textContent with whoevers turn it is currently

save the battle state as battle begins. battle state should contain the current player.username and monster.name
along with the date and players.level and monster level and status as "started battle"

if player wins:
we save the battle state like before but we also add monster.xp and monster.yen and monster.items,
and change the status to "player.username wins"
change the text content of "rewards" div to add monster.xp and monster.yen and monster.items and set display to block


other notes:
could use the $inc method in mongo db with a fetch post to the database to increase a players stats or money after the battle

kind of want to put the battle state "schema" inside this function and send it to the backend

battle function can either start a battle with a specific monster or pull a random monster from the database

OUTLINE:

let player;
let monster;

define variables outside the functions so they can all use it
then in the start battle function we can fill in these variables and they will have data since this function is called first 

function startBattle(){
    fetch call player
    assign data to player variable
    fetch call monster
    assign data to monster variable
    fill in player and enemy stats and pictures in battle html
    make the battle log say 'battle initiated - player vs monster - nightcore database updated - battle start! '
    send a battle object (schema) to a battles collection inside the nightcore database
    activate player turn
}
function playerTurn(){
    change the color of the choice box to a light color so we know its our turn
    update the turn box to say players turn (dynamic=req.session.players.name)
}

